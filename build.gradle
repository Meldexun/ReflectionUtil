apply plugin: 'java'
apply plugin: 'eclipse'

jar {
    archiveBaseName = 'ReflectionUtil'
    archiveVersion = '1.0.0'
    manifest.attributes([
        'Built-By': 'Meldexun',
        'Specification-Title': 'ReflectionUtil',
        'Specification-Version': '1.0.0',
        'Specification-Vendor': 'Meldexun',
        'Implementation-Title': 'ReflectionUtil',
        'Implementation-Version': '1.0.0',
        'Implementation-Vendor': 'Meldexun',
        'Implementation-Timestamp': new Date().format("yyyy-MM-dd HH:mm:ss")
    ])
}
